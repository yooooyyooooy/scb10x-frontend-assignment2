import type { NextPage } from 'next';
import Head from 'next/head';
import Image from 'next/image';

const Home: NextPage = () => {
      return <div className="text-red-600 font-bold">Leverage</div>;
};

export default Home;
