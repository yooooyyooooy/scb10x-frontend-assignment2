import React from 'react';
import MainTemplate from '@templates/MainTemplate';
import CloseLeverage from '@modules/CloseLeverage';

const PositionLayout = () => {
      return (
            <MainTemplate>
                  <CloseLeverage />
            </MainTemplate>
      );
};

export default PositionLayout;
